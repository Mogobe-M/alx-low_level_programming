#include <stdio.h>
#include <stdlib.h>

/**
 *main-
 *discription
 *returns 0
 **/

int main()
{
  int ch;
  int ch =97
    /*for lowercase alphabets*/
    for (ch 97; ch <= 122; ch++)
      {
       putchar(ch);
      }
	/*for uppercase alphabets*/
    for (capch =65; capch <= 90; capch++)
      {
       ptchar(capch);
      }
    return(0);
}
