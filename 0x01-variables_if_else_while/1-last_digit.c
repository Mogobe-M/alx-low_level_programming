#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 *main-print if the number is greater or lesser
*discription:using the main function
*this program prints 'programing is ositive, zero or negative'
*returns 0
**/
int main(void)
{
  int n;
  int l;
  srand(time(0))
  l = n % 10

    if (l > 5)
      {
       print f("last digit of %d is %d and is greater than 5\n", n, l);
      }
    else if (l ==0)
      {
       printf("last digit of %d is %d and is 0\n", n, l)
      }
    else (n < 6)
	   {
	    printf("last digit of %d is %d and is less than 6\n", n, l);
	   }
  return (0);
}
